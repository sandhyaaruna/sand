#include<conio.h>
#include<stdio.h>
void main()
{
int n,a=0,remainder,org int;
printf("enter an int");
scanf("%d",&n);
original integer=n;
while(n!=0)
{
rem=n%10
rev int=rev in*10 +rem;
n/=10;
}
if(org int==rev int)
{
printf("%d palindrome",org int);
}
else
{
printf("%d not palindrome", org int);
}
getch();
}
